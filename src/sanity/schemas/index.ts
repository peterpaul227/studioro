import { project } from "./project";
import { service } from "./service";
import { client } from "./client";
import { siteSettings } from "./siteSettings";

export const schemaTypes = [project, service, client, siteSettings];
